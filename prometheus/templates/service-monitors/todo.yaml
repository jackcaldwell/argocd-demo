apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: todo
  namespace: {{ template "kube-prometheus-stack.namespace" . }}
  labels:
    app: todo
  # labels:
  #   team: frontend
spec:
  selector:
    matchLabels:
      app: todo
  endpoints:
    - path: /metrics
      port: web
  namespaceSelector:
    matchNames:
      - todo